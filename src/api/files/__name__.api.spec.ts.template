describe('<%= name %>のテスト', () => {
    const testRequestClient = {};
    const testRequestClientSpy = jest.fn().spyOn(TestRequestClient.prototype, 'exec');

    afterEach(() => {
        testRequestClientSpy.mockReset();
    });

    describe('正常系', () => {
        test('', async () => {});
    })
    describe('異常系', () => {
        test('', async () => {});
    })
});
